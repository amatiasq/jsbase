{
	"name": "jsbase",
	"version": "1.0.4",
	"description": "A base library to develop easily classes and modules",
	"author": "A. Matías Quezada <amatiasq@gmail.com>",
	"dependencies": {},
	"devDependencies": {
		"unused": "0.2.x",
		"leaky": "0.1.x",
		"mocha": "1.7.x",
		"sinon": "1.5.x",
		"promise-tests": "*"

	},
	"scripts": {
		"test": "unused src/* && leaky src && promise-tests all test/promise-tests-adapter && promise-tests all test/promise.es5-tests-adapter && mocha",
		"install": "git clone git://github.com/visionmedia/node-jscoverage.git && cd node-jscoverage && ./configure && make"
	}
}
